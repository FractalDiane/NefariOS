[gd_resource type="Resource" load_steps=2 format=2]

[ext_resource path="res://scripts/files/nos_text_file.gd" type="Script" id=1]

[resource]
script = ExtResource( 1 )
file_name = "Tutorial.txt"
can_be_corrupted = false
is_secret = false
text = "- Left click a file or directory to open
     it. .pcx files will open images, and
	.txt and several other extensions
	will open text files.
- Drag the window around to move it.
- When you find a target file, right 
    click on it and click \"transfer.\"
- Make sure the virus doesn't corrupt 
    too many target files.

The virus progresses every time you change directories,
but not when a file is opened.

Look at the light at the bottom right 
for an indication of how many of them 
are corrupted."
